# This is the main file that will run the entire program.
# It will call the encryption function, then the ui function,
# and the decryption function.

# pip install cryptography 
# make sure this is installed before running the program

import os
import sys
import secrets
import argparse
